<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Toy</title>
    <!-- <link rel="stylesheet" href="/styles.css"> -->
    <style>
        /* General Styling */
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
        }

        /* Container */
        .container {
            max-width: 40%;
            margin: 50px auto;
            background: white;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        /* Heading */
        h2 {
            color: #333;
            font-size: 24px;
            margin-bottom: 15px;
        }

        /* Image */
        img {
            width: 100%;
            max-width: 250px;
            border-radius: 10px;
            box-shadow: 0px 2px 5px rgba(0, 0, 0, 0.2);
            margin-bottom: 15px;
        }

        /* Toy Details */
        h3 {
            font-size: 22px;
            color: #007BFF;
            margin-bottom: 10px;
        }

        p {
            font-size: 18px;
            color: #444;
            margin: 8px 0;
        }

        strong {
            color: #333;
        }

        /* Back Button */
        .btn {
            display: inline-block;
            padding: 10px 15px;
            margin-top: 15px;
            text-decoration: none;
            background: #007BFF;
            color: white;
            font-size: 16px;
            border-radius: 5px;
            transition: background 0.3s ease;
        }

        .btn:hover {
            background: #0056b3;
        }
    </style>
</head>

<body>
    <div class="container">
        <h2>View Toy Details</h2>
        <img src="<%= toys.ProductImageURL %>" width="200" alt="<%= toys.ProductName %>">
        <h3>
            <%= toys.ProductName %>
        </h3>
        <p><strong>Category:</strong>
            <%= toys.Category %>
        </p>
        <p><strong>Price:</strong> ₹<%= toys.Price %>
        </p>
        <a href="/toys/adminToys" class="btn">Back to Dashboard</a>
    </div>

</body>

</html>