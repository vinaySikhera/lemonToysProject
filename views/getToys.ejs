<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <title>all Toys</title>
    <link rel="shortcut icon" href="/images/lemontoyslogo.jpeg" type="image/x-icon">
    <link rel="stylesheet" href="css/navbar.css">
    <style>
        .productsBoxContainer {
            display: flex;
            flex-wrap: wrap;
            gap: 25px;
            align-items: center;
            justify-content: center;
        }

        .card {
            width: 250px;
            border: 1px solid #ddd;
            border-radius: 10px;
            overflow: hidden;
            padding: 10px;
            margin: auto;
            box-shadow: 2px 2px 10px rgba(0, 0, 0, 0.1);
            position: relative;
        }

        .sale-badge {
            background: red;
            color: white;
            padding: 5px 10px;
            font-size: 12px;
            border-radius: 5px;
            position: absolute;
            top: 10px;
            left: 10px;
        }

        .image-container img {
            width: 100%;
            border-radius: 5px;
        }

        .price {
            color: green;
            font-size: 18px;
            font-weight: bold;
        }

        .old-price {
            text-decoration: line-through;
            color: grey;
            margin-left: 5px;
        }

        .title {
            font-size: 16px;
            margin: 10px 0;
        }

        .icons {
            position: absolute;
            top: 10px;
            right: 10px;
            display: flex;
            gap: 10px;
        }

        .icons span {
            font-size: 20px;
            cursor: pointer;
        }

        .checked {
            color: gold;
        }
    </style>
</head>

<body>
    <%- include('common/navbar.ejs') %>
        <h1>get all toys here</h1>
        <div>
            <h1>filter by price</h1>
            <input type="range" name="range" id="range" min="10" max="1000">
        </div>
        <div class="productsBoxContainer">
            <% getAllToys?.forEach(product=> { %>
                <div class="card">
                    <span class="sale-badge">SALE</span>
                    <div class="icons">
                        <span>&#x2661;</span>
                        <span>&#128722;</span>
                    </div>
                    <div class="image-container">
                        <% if (product.imageUrl) { %>
                            <img src="<%=HOST%>:/<%= product.imageUrl %>" alt="<%= product.name %>">
                            <% } else { %>
                                <img src="https://via.placeholder.com/200" alt="No Image Available">
                                <% } %>
                    </div>
                    <h3 class="title">
                        <%= product.name %>
                    </h3>
                    <span class="price">₹<%= product.price %></span>
                    <span class="old-price">₹<%= product.price + 40 %></span>
                </div>
                <% }); %>
        </div>

</body>

</html>