<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Toy</title>
    <style>
        /* General Styling */
        body {
            /* border: 10px solid black; */
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
            display: flex;
            justify-content: center;
            align-items: center;
            /* height: 100vh; */
            box-sizing: border-box;
        }

        /* Container */
        .container {
            width: 90%;
            max-width: 600px;
            background: white;
            margin-top: 50px;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        /* Heading */
        h2 {
            color: #333;
            font-size: 24px;
            margin-bottom: 15px;
        }

        /* Form */
        form {
            display: flex;
            flex-direction: column;
        }

        /* Labels */
        label {
            text-align: left;
            font-size: 16px;
            color: #333;
            margin-bottom: 5px;
        }

        /* Input Fields */
        input {
            width: 100%;
            padding: 10px;
            margin-bottom: 15px;
            border: 1px solid #ccc;
            border-radius: 5px;
            font-size: 16px;
        }

        /* Submit Button */
        .btn {
            display: inline-block;
            padding: 10px 15px;
            background: #007BFF;
            color: white;
            font-size: 16px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background 0.3s ease;
        }

        .btn:hover {
            background: #0056b3;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            body {
                padding: 10px;
            }

            .container {
                width: 100%;
                padding: 15px;
            }

            h2 {
                font-size: 20px;
            }

            .btn {
                font-size: 14px;
                padding: 8px 12px;
            }

            label {
                font-size: 14px;
            }

            input {
                font-size: 14px;
                padding: 8px;
            }
        }

        @media (max-width: 480px) {
            h2 {
                font-size: 18px;
            }

            .btn {
                font-size: 14px;
                padding: 8px;
            }

            input {
                font-size: 14px;
                padding: 8px;
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <h2>Edit Toy</h2>
        <form action="/updateToy/<%= toy._id %>" method="POST">
            <label for="ProductName">Product Name:</label>
            <input type="text" name="ProductName" value="<%= toy.ProductName %>" required>

            <label for="Category">Category:</label>
            <input type="text" name="Category" value="<%= toy.Category %>" required>

            <label for="Price">Price:</label>
            <input type="number" name="Price" value="<%= toy.Price %>" required>

            <label for="ProductImageURL">Image URL:</label>
            <input type="text" name="ProductImageURL" value="<%= toy.ProductImageURL %>" required>

            <label for="ProductDescription">Product Description:</label>
            <input type="text" name="ProductDescription" value="<%= toy.ProductDescription %>" required>

            <label for="PriceA">PriceA:</label>
            <input type="text" name="PriceA" value="<%= toy.PriceA %>" required>

            <label for="PriceB">PriceB:</label>
            <input type="text" name="PriceB" value="<%= toy.PriceB %>" required>

            <label for="PriceC">PriceC:</label>
            <input type="text" name="PriceC" value="<%= toy.PriceC %>" required>

            <label for="PriceD">PriceD:</label>
            <input type="text" name="PriceD" value="<%= toy.PriceD %>" required>

            <button type="submit" class="btn">Update Toy</button>
        </form>
        <br>
        <a href="/adminToys" class="btn">Back to Dashboard</a>
    </div>
</body>

</html>